{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"build\"",
    "deploy": "echo \"deploy\"",
    "api:build": "yarn build",
    "api:deploy": "yarn deploy"
  }
}
