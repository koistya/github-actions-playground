{
  "name": "app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"build\"",
    "deploy": "echo \"deploy\"",
    "app:build": "yarn build",
    "app:deploy": "yarn deploy"
  }
}
